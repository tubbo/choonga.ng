linkTo 'links' class="title" | choonga.ng
span &nbsp;/&nbsp;
linkTo 'comments' | comments
span &nbsp;/&nbsp;
if current_user
  linkTo 'links.submit' | post a link
else
  modal href='/login' text='post a link'
span &nbsp;/&nbsp;
Choongang.UserAuthView
